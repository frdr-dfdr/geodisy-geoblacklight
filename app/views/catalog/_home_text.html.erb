<div class='container' style="font-size:14px; margin-bottom:1rem; max-width: 534px">
  <div class='row text-center'>
    <div class='col-sm'>
      		<div>
			<%= content_tag :h3, t('geoblacklight.home.map_heading') %>
	      		<%= content_tag :div, '', id: 'map', data: { map: 'home', 'catalog-path'=> search_catalog_path , 'map-bbox' => params[:bbox], basemap: geoblacklight_basemap, leaflet_options: leaflet_options } %>
		<div id="map_instructions">
			<div><%= t('geoblacklight.home.use_map') %></div>
			<div style="padding-left:1rem; text-indent:-1rem">1. <%= t('geoblacklight.home.move_map') %></div>
			<div>2. <%= t('geoblacklight.home.click') %> <%= t('geoblacklight.home.left_quote') %><%= t('geoblacklight.home.search_here') %><%= t('geoblacklight.home.right_quote') %> <%= t('geoblacklight.home.results') %></div>
		</div>
    </div>
  </div>
</div>
